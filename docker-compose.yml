services:
  backend:
    image: ${DOCKERHUB_NAMESPACE:-incidentzero}/incidentzero-backend:${IMAGE_TAG:-latest}
    build: ./backend
    container_name: incidentzero-backend
    ports:
      - "8000:8000"
    environment:
      - ENV=production
      - DEMO_MODE=true
      - DATABASE_URL=sqlite:///./incidentzero.db
      - CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://frontend:80
      - ELASTIC_ENABLED=${ELASTIC_ENABLED:-false}
      - ELASTIC_URL=http://elasticsearch:9200
      - ELASTIC_USERNAME=elastic
      - ELASTIC_PASSWORD=changeme
      - GEMMA_ENABLED=${GEMMA_ENABLED:-false}
      - OLLAMA_URL=http://ollama:11434
      - GEMMA_MODEL=${GEMMA_MODEL:-gemma4}
    volumes:
      - ./backend/datasets:/app/datasets
    networks:
      - incidentzero-net

  frontend:
    image: ${DOCKERHUB_NAMESPACE:-incidentzero}/incidentzero-frontend:${IMAGE_TAG:-latest}
    container_name: incidentzero-frontend
    ports:
      - "5173:80"
    build:
      context: ./frontend
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:8000}
    depends_on:
      - backend
    networks:
      - incidentzero-net

  # ---- Optional: real Elasticsearch cluster ----
  # Enable with:  docker compose --profile elastic up
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.15.1
    container_name: incidentzero-elasticsearch
    profiles: ["elastic"]
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    ports:
      - "9200:9200"
    volumes:
      - es-data:/usr/share/elasticsearch/data
    networks:
      - incidentzero-net

  # ---- Optional: local Gemma runtime via Ollama ----
  # Enable with:  docker compose --profile gemma up
  # Then: docker exec -it incidentzero-ollama ollama pull gemma4
  ollama:
    image: ollama/ollama:latest
    container_name: incidentzero-ollama
    profiles: ["gemma"]
    command: ["serve"]
    ports:
      - "11434:11434"
    environment:
      - OLLAMA_HOST=0.0.0.0
    volumes:
      - ollama-data:/root/.ollama
    networks:
      - incidentzero-net

networks:
  incidentzero-net:
    driver: bridge

volumes:
  es-data:
  ollama-data:
